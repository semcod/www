# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: badge.spec.js >> Badge Generator >> generates markdown for repo
- Location: e2e/badge.spec.js:4:3

# Error details

```
Error: page.goto: net::ERR_CONNECTION_REFUSED at http://localhost:5173/#tab=badge
Call log:
  - navigating to "http://localhost:5173/#tab=badge", waiting until "load"

```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test.describe('Badge Generator', () => {
  4  |   test('generates markdown for repo', async ({ page }) => {
> 5  |     await page.goto('/#tab=badge');
     |                ^ Error: page.goto: net::ERR_CONNECTION_REFUSED at http://localhost:5173/#tab=badge
  6  |     
  7  |     await expect(page.getByText('Code Health Badge')).toBeVisible();
  8  |     
  9  |     // Clear and enter new repo using placeholder
  10 |     const input = page.getByRole('textbox').first();
  11 |     await input.fill('myorg/myrepo');
  12 |     
  13 |     // Markdown should update - look for partial match
  14 |     await expect(page.getByText(/myorg-myrepo/)).toBeVisible();
  15 |   });
  16 | 
  17 |   test('shows grade scale', async ({ page }) => {
  18 |     await page.goto('/#tab=badge');
  19 |     
  20 |     // Check for grade scale section with text
  21 |     await expect(page.getByText('Grade Scale')).toBeVisible();
  22 |     
  23 |     // Check individual grade circles exist
  24 |     const grades = ['A+', 'A', 'B+', 'B', 'C', 'D', 'F'];
  25 |     for (const grade of grades) {
  26 |       // Look for grade in the context of the grade scale
  27 |       const gradeElement = page.locator('text=' + grade).filter({ hasText: grade }).first();
  28 |       await expect(gradeElement).toBeVisible();
  29 |     }
  30 |   });
  31 | });
  32 | 
```